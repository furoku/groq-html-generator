# Copy this file to .env and fill values

# Required: Groq API key
GROQ_API_KEY=your_api_key_here

# Optional: Express server port (defaults to 3000)
PORT=3000

# Optional: Model and sampling (fast default)
# GROQ_MODEL=llama-3.1-8b-instant
# GROQ_TEMPERATURE=1
# GROQ_TOP_P=1

# Optional: Offline mock mode (no API calls)
# GROQ_MOCK=1

# Optional: Hedged requests to reduce tail latency
# Start a backup request if no first token within N ms
# GROQ_HEDGE_MS=250
# GROQ_HEDGE_MODEL=llama-3.1-8b-instant
